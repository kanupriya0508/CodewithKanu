git config -- global user.name
git config -- global user.email
git status
git init
git add --a
git add .
git add filename
git commit -m "message"
rm -rf .git
git diff (compares working dir with staging area)
git diff --staged (compares staging area with last commit)
git commit -a -m "direct commit" (commits without placing modified flies in staging area directly, but if there is any untracket file, it wont commit without staging area)
git rm filename.txt 
git mv first.txt first1.txt (renames flie from first.txt to first1.txt)
git rm --cached filename (stops tracking that file, wont delete it, it will be shown in untracked file, if you put this in .gitignore it will stop showing untracked file.. but .gitignore cannot ignore files whire already being tracked.. you have to first explicitly untrack it)
git clone url foldername
git log (shows all commits)
git log -p (changes)
git log -p -n (n =3/4 /5 etc)
git log --stat (shows all commits with short summary)
git log --pretty=oneline
git log --pretty=short
git log --pretty=full
git log --pretty=format:"%h -- %an" ---> diff formats go to scm doc
author : who made file commiter : who modifies file
git log --since=2.days/weeks/months/years
git commit --amend (to add change to previous commit)
git restore --staged filename (to unstage)
git checkout -- filename (to unmodify a file) (eg : if i deleted a para from first.txt, but i want it back , then use this )
git checkout -f (to unmodify all the changes you made and go to previous commit)
checkout wont work for files in staged area. will work only for changes not staged yet.





